/**
 * @deprecated Usar `@/lib/store-settings` (`buildWhatsAppUrl`, `mapPublicStoreSettings`).
 * Se mantiene solo por compatibilidad temporal.
 */
export { buildWhatsAppUrl } from "@/lib/store-settings";
