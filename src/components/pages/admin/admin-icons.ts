import type { IconType } from "react-icons";
import {
  FiHome,
  FiPackage,
  FiGrid,
  FiShoppingCart,
  FiUsers,
  FiSettings,
} from "react-icons/fi";

export const ADMIN_ICON_MAP: Record<string, IconType> = {
  FiHome,
  FiPackage,
  FiGrid,
  FiShoppingCart,
  FiUsers,
  FiSettings,
};
